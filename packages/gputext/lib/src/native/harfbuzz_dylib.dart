// Marker library for the bundled HarfBuzz code asset.
// The build hook registers `gputext_harfbuzz` as
// `package:gputext/src/native/harfbuzz_dylib.dart`; bindings load it via
// `@Native(assetId: ...)` (preferred) or a narrow DynamicLibrary fallback.
library;
