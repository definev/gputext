// Marker library for the bundled system-font resolver code asset.
// The build hook registers `gputext_system_fonts` as
// `package:gputext/src/native/system_fonts.dart`; bindings load it via
// `@Native(assetId: ...)` (preferred) or a narrow DynamicLibrary fallback.
//
// Companion of `harfbuzz_dylib.dart`; see `system_fonts.c` for the native side.
library;
